import XCTest
@testable import VocableLocalizationTest

final class VocableLocalizationTestTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(VocableLocalizationTest().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
